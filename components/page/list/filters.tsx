export function Filters({setIsFiltersOpen, isFiltersOpen}: any) {
    return (
        <div className={`transition-all w-full ${isFiltersOpen ? 'h-[100px]' : 'h-0 overflow-hidden' }`}>
            test
        </div>
    )
}